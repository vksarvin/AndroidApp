package com.voltuswave.roomtempo.shared.interfaces

import android.net.Uri

interface FragmentInteractionListener {

    // TODO: Update argument type and name
    fun onFragmentInteraction(uri: Uri)
}